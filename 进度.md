# 项目进度日志

> 配套文档：[开发大纲.md](./开发大纲.md)
> 每完成一步就在本文件末尾追加；遇到决策变更，新增"决策"段落；阻塞写"待解"。

---

## 总体框架（确认稿）

**产品形态**：Web 应用（浏览器访问）。用户最终只需打开网页 → 登录 GitHub → 跟 AI 对话造应用。
**部署模式**：后端服务 + 前端静态资源。开发期跑在本服务器，上线期可部到任何能反代 GitHub API 的地方。
**真相源**：所有用户代码、密钥、运行记录都在用户自己的 GitHub 仓库；后端不持久化业务数据。

### 架构图

```
浏览器 (Vue 前端)
   │  REST + SSE
   ▼
FastAPI 后端 (无业务数据库)
   ├── AI Provider 适配  ─────► DeepSeek / Claude / 通义
   ├── GitHub API 封装   ─────► api.github.com
   └── Agent Loop       (工具调用：读/写文件、跑 Actions、读日志)
```

### 顶层目录

```
app-for-oneself/
├── 开发大纲.md            # 设计文档（稳定不动）
├── 进度.md                # 本文件
├── README.md              # 项目说明
├── requirements.txt       # Python 依赖
├── .env.example           # 环境变量模板
├── backend/               # FastAPI 后端
│   └── app/
│       ├── main.py        # 应用入口
│       ├── core/          # 配置、日志、异常
│       ├── api/           # REST + SSE 端点
│       ├── ai/            # AI provider + agent loop
│       │   └── providers/ # deepseek / claude / qwen
│       ├── github/        # GitHub API 封装
│       └── models/        # Pydantic 数据模型
├── frontend/              # Vue 3 + Vite
│   └── src/
│       ├── views/         # 引导/工作台/密码本/日志
│       ├── components/
│       ├── api/           # 调后端
│       └── stores/        # Pinia
├── templates/             # 项目模板（提供给用户 fork）
│   └── static-site/       # 第一个：静态网页
├── scripts/               # 一次性脚本
└── docs/                  # 用户面向文档（少而精）
```

### 关键技术选型

| 模块 | 选型 | 备注 |
|---|---|---|
| 后端 | FastAPI + uvicorn | async、SSE 支持好 |
| Python | 3.11（conda appdev） | — |
| HTTP | httpx | async；GitHub + AI 都用它 |
| GitHub SDK | 自己用 httpx 封装 + PyGithub 兜底 | PyGithub 同步，主链路用 httpx |
| AI 调用 | OpenAI SDK 兼容协议（DeepSeek 支持） | 后续 Claude 用 anthropic SDK |
| 前端 | Vue 3 + Vite + Element Plus + Pinia | 中文生态最熟 |
| 前后端通信 | REST（命令）+ SSE（流） | WebSocket 暂不引入 |
| 本地存储 | 浏览器 localStorage（凭据）+ 用户 GitHub 仓库（业务数据） | 后端无 DB |

### 环境前置

- [x] Python 3.11.15 in `conda appdev`
- [x] GitHub API 可达（200, 0.5s）
- [x] DeepSeek API 可达（401，需 Key）
- [ ] 用户提供 `DEEPSEEK_API_KEY`
- [ ] 用户提供 `GITHUB_TOKEN`（fine-grained PAT，权限：repo + workflow + secrets）
- [ ] 用户提供一个可丢弃的测试仓库名

---

## 进度时间线

### 2026-05-24 · M0-1 项目骨架 ✅

**做了什么**

- 顶层目录结构搭好：`backend/{api,ai/providers,github,models,core}`、`frontend/src/{views,components,api,stores}`、`templates/`、`scripts/`、`docs/`
- 文档：`进度.md`、`README.md`、`.gitignore`
- 配置：`requirements.txt`（fastapi/httpx/openai/anthropic/loguru/sse-starlette 等）、`.env.example`
- 后端最小骨架：
  - `backend/app/core/config.py`：`pydantic_settings` 读 `.env`，所有配置集中管理
  - `backend/app/core/logging.py`：loguru 彩色日志
  - `backend/app/main.py`：FastAPI 入口 + CORS + `/healthz`
- 依赖安装：`conda appdev` 里 `pip install -r requirements.txt` 通过
- 烟雾测试：`uvicorn backend.app.main:app --port 8901` 成功，`/healthz` 返回
  `{"status":"ok","env":"dev","deepseek_configured":false,"github_configured":false}`

**踩坑**

- 端口 8765 被服务器上别的服务占了（且返回 `{"error":"not found"}` 不是 FastAPI 的 404 格式）→ 改用 8901
- `libtinfo.so.6` warning 来自用户 miniconda 与系统 bash 版本不匹配，不影响功能，忽略

**接下来**

- M0-2：实现 GitHub API 封装层（先实现读 + 提交 + dispatch workflow + 读 logs 四个核心操作）

---

### 2026-05-24 · M0-2 GitHub API 封装层 ✅

**做了什么**

- 凭据写入 `.env`：DeepSeek key、GitHub PAT、测试仓库 `McCoyLee/github-api-test`
- 三方连通性验证通过：GitHub user=McCoyLee + repo 全权限；DeepSeek `chat/completions` 返回 OK
- 新增依赖：`httpx[socks]`（服务器走 SOCKS 代理）、`PyNaCl`（GitHub Secrets 必需 libsodium sealed box）
- `backend/app/github/`：
  - `errors.py`：`GitHubError` / `GitHubAuthError` / `GitHubNotFound` / `GitHubRateLimited`，按 HTTP 状态码分诊
  - `client.py`：异步 `GitHubClient`，覆盖
    - meta：`whoami` / `get_repo`
    - 文件：`read_file` / `write_file`（含 sha 更新）/ `list_dir` / `search_code`
    - 分支与 PR：`get_branch` / `create_branch` / `ensure_branch` / `open_pr` / `merge_pr` / `close_pr` / `delete_branch`
    - 工作流：`list_workflows` / `dispatch_workflow` / `list_runs` / `get_run` / `get_run_logs`（zip）
    - 密码本：`_repo_public_key` / `list_secrets` / `set_secret`（NaCl 加密）/ `delete_secret`
    - 变量：`list_variables` / `set_variable` / `delete_variable`
- `scripts/smoke_github.py`：对真实仓库跑了 12 步端到端测试，包含自清理（关 PR + 删分支）

**烟雾测试结果**（真实跑 `McCoyLee/github-api-test`）

```
✓ 1-8  whoami/get_repo/list_dir/branch/write/read/update/PR  全部通过
✓ 9    secrets set/list/delete                                 通过
✗ 10   variables                                                403 缺权限
✓ 11   list_workflows                                           1 个（Copilot）
✓ 12   cleanup（关 PR + 删分支）                                通过
```

**踩坑**

- httpx 默认不带 socks，服务器 `ALL_PROXY=socks5h://127.0.0.1:7890` 触发 `ImportError` → 装 `httpx[socks]` 解决
- fine-grained PAT 的 `Secrets` 和 `Variables` 是**两个独立权限位**，当前 token 缺 Variables。**M0 不阻塞**（核心走 secrets）；后续 UI 引导用户授权时要分开说明
- 真实仓库初始没有 `actions/variables` 接口的访问能力时，`set_variable` 内部 GET-then-decide 模式会先吃一个 403——预期行为，外层用 `_step` 包了

**遗留**

- M1：让 token 检查时主动探测 Variables/Secrets 权限，UI 上分别标"已授权 / 未授权"

**接下来**

- M0-3：AI Provider 抽象 + DeepSeek 实现 + agent loop（function calling → 调 GitHub 工具）

---

### 2026-05-24 · M0-3 AI Provider 与 Agent 循环 ✅

**做了什么**

- `backend/app/ai/providers/base.py`：`Provider` 协议 + `AssistantMessage` / `ToolCall` dataclass
- `backend/app/ai/providers/deepseek.py`：`DeepSeekProvider`，走 OpenAI SDK 异步客户端，base_url 指向 `api.deepseek.com`
- `backend/app/ai/tools.py`：
  - 10 个工具的 OpenAI 风格 schema：`read_file` / `list_dir` / `search_code` / `write_file` / `ensure_branch` / `open_pr` / `dispatch_workflow` / `list_workflows` / `list_runs` / `list_secrets`
  - dispatcher：tool name → `GitHubClient` 方法，统一返回 `{"ok": bool, "result"|"error"}`，异常吞掉转字符串让 AI 自己反思
  - 安全护栏：`write_file` 和 `ensure_branch` 强制分支名以 `ai/` 开头
- `backend/app/ai/prompts.py`：中文 system prompt，明确铁律（不碰 main、必须 PR、sha 校验、不泄漏密钥）
- `backend/app/ai/agent.py`：
  - `Agent.run(history)` 异步生成器，产出 `AgentEvent`（assistant_text / tool_call / tool_result / usage / done / error）
  - 最多 8 轮迭代防失控
  - 累计 token 用量

**端到端跑通**（真实 DeepSeek + 真实 GitHub）

任务：「在根目录新建 hello-from-ai.md，欢迎语 + 时间戳，新分支 ai/hello-XXX，提交并开 PR」

```
iter 0: list_dir('')               → 见到 README.md
iter 1: ensure_branch('ai/hello-1779593973', 'main')  → sha
iter 2: write_file(...)            → commit a89819d
iter 3: open_pr(...)               → PR #4
iter 4: 总结回复（含 PR 链接的 markdown 表格）

usage = {prompt: 10281, completion: 591, total: 10872}
finish_reason = stop
耗时 ~11s
```

**观察**

- DeepSeek 工具调用稳定，schema 理解准确，主动遵守了"先 list_dir 再写"的铁律
- system prompt 中"完成后告诉用户"被执行得很好（输出了带链接的 markdown 表格）
- 一次完整任务约 10k input tokens（工具 schema 占大头），后续可考虑按场景裁剪工具集合

**遗留**

- prompt cache：DeepSeek 已经支持，目前 `prompt_cache_hit_tokens = 0`，下一轮加 system prompt 缓存优化
- agent 没有 streaming 输出（一轮完了一起吐），M0-4 SSE 接入时改成流式
- cleanup 脚本里用了 `gh._request` 私有方法，后面在 client 加 `list_open_prs(head=...)` 公开方法

**接下来**

- M0-4：FastAPI 路由 + SSE 把 agent 流式接到前端

---

### 2026-05-24 · M0-4 FastAPI 路由 + SSE ✅

**做了什么**

- `backend/app/api/deps.py`：依赖注入
  - `require_provider`：返回缓存的 DeepSeek provider（lru_cache 单例）
  - `github_client`：每请求一个 `GitHubClient` 实例，结束自动 close
  - `require_repo`：查询参数优先，否则用 `.env` 的 `TEST_REPO`
- `backend/app/api/repo.py`：`/api/repo/info` / `/tree` / `/file`
- `backend/app/api/secrets.py`：`GET /api/secrets`、`POST /api/secrets`（Pydantic 校验 NAME 格式）、`DELETE /api/secrets/{name}`
- `backend/app/api/actions.py`：`GET /api/workflows`、`GET /api/runs`、`POST /api/runs/dispatch`
- `backend/app/api/chat.py`：`POST /api/chat` SSE 流式，把 `Agent.run` 的事件用 `sse_starlette.EventSourceResponse` 推给前端
- `main.py` include 全部 router

**端到端验证**

```
✓ /healthz           → deepseek_configured/github_configured 都 true
✓ /api/repo/info     → user=McCoyLee, repo=github-api-test, default=main
✓ /api/repo/tree     → README.md
✓ /api/secrets       → []
✓ /api/workflows     → Copilot
✓ /api/chat (SSE)    → 完整跑通：list_dir → ensure_branch → write_file → open_pr (#5)
                       usage = 10606 tokens，事件流式输出
                       自清理 PR #5 + 分支 ai/sse-XXX
```

**关键决策**

- SSE 协议：用 `sse_starlette.EventSourceResponse`，事件格式 `event: <type>\ndata: <json>\n\n`
- 错误统一：`GitHubError` → HTTPException（保留状态码与 message）
- 业务数据全靠 GitHub，**FastAPI 完全无状态**（即使重启服务也不丢任何用户数据）

**遗留**

- token 现在只从 `.env` 读，多用户场景要从请求头 `Authorization` / 自定义 `X-GH-Token` 拿，M1 改
- `chat.py` 接收的历史消息没有持久化，前端要自己维护对话历史；这是设计选择（仓库即数据库），后续可选地存到 `.ai/sessions/*.jsonl` 仓库内文件

**接下来**

- M0-5：Vue 前端最小界面（引导页 + 工作台聊天 + secrets 管理 + 工作流面板）

---

### 2026-05-24 · M0-5 Vue 前端最小可用界面 ✅

**做了什么**

- 选型：Vue 3 + Vite 6 + Element Plus 2.8 + Pinia 2.2 + marked 14（渲染 AI 输出的 markdown）
- `frontend/package.json`、`vite.config.js`（dev server 5173，`/api` 和 `/healthz` 都代理到 8901）
- `frontend/src/main.js`：注册 Element Plus + 全量图标 + Pinia
- `frontend/src/api/`：
  - `client.js`：axios 封装 `repoApi` / `secretsApi` / `actionsApi` / `health`
  - `chat.js`：用 `fetch + ReadableStream` 实现 SSE 客户端（EventSource 只支持 GET，我们用 POST），手动解析 `event:` / `data:` 行
- `frontend/src/stores/session.js`：Pinia store 维护 `repo` / `user` / `messages` / `usage`
- `frontend/src/App.vue`：顶栏（用户/仓库/凭据状态徽章），下方主区
- `frontend/src/views/Workspace.vue`：左聊天右标签页（仓库 / 密码本 / 运行）布局
- `frontend/src/components/`：
  - `ChatPanel.vue`：消息流（user/assistant/tool 三种气泡），AI 用 marked 渲染 markdown，工具调用以可展开卡片展示（绿/红边表示成功/失败），实时累计 token
  - `RepoTree.vue`：仓库浏览（面包屑导航、点目录进入、点文件弹窗看内容）
  - `SecretsPanel.vue`：密码本（列表 + 添加 + 删除，强制大写下划线命名）
  - `RunsPanel.vue`：工作流列表 + 一键 dispatch + 最近运行表格

**验证**

```
✓ npm install         → 包齐全
✓ npm run build       → 生产构建成功，1668 modules，~1.2MB
✓ uvicorn :8901       → /healthz 200
✓ vite dev :5173      → / 200
✓ proxy /healthz      → {"status":"ok",...}
✓ proxy /api/repo/info → 拿到 McCoyLee/github-api-test 信息
```

**待人肉验证（M0-6 一起做）**

- 浏览器打开 `http://<server>:5173/`
- 顶栏应显示绿色 `DeepSeek ✓` `GitHub ✓`，用户头像，仓库名
- 聊天框输入"在 README 末尾加一行 hello"，观察 AI 工具流以卡片形式实时流出
- 密码本能添加 / 删除（PAT 已有 secrets 权限）
- 运行面板能看到 Copilot workflow

**接下来**

- M0-6：端到端联调 + 加一个真正能跑的静态网页模板（自带 GitHub Actions 把 index.html 推 Pages，让用户对 AI 说"造一个个人主页"全程跑通）

---

### 2026-05-24 · M0-6 端到端联调 ✅

**做了什么**

- `templates/static-site/`：第一个完整模板
  - `index.html` / `style.css` / `main.js` 三件套
  - `.github/workflows/pages.yml`：push 到 main 自动部署 GitHub Pages
  - `AGENTS.md`：约束 AI 别引入构建工具、保持三文件结构、用 CSS 变量
  - `README.md`：给用户看的使用说明
- `scripts/init_template.py`：把模板推到测试仓库 main（已对 `McCoyLee/github-api-test` 执行成功，6 文件就位）
- 启动了 `uvicorn :8901` + `vite dev :5173`，前后端代理打通

**端到端实测**（通过 SSE 端点）

任务："把首页标题改成 App For Oneself 演示站，强调色改成 #10b981，开 PR"

```
list_dir(.)
  ↓
read_file(index.html)     ← AI 主动读上下文
read_file(style.css)
  ↓
ensure_branch(ai/update-title-and-accent, from=main)
  ↓
write_file(index.html, sha=...)  ← 正确传 sha
write_file(style.css, sha=...)
  ↓
open_pr → #6
  ↓
返回带 markdown 表格的总结

usage: 17791 + 1178 = 18969 tokens
```

**M0 整体完成情况**

| 任务 | 状态 |
|---|---|
| M0-1 项目骨架 | ✅ |
| M0-2 GitHub API 封装 | ✅ |
| M0-3 AI Provider + Agent loop | ✅ |
| M0-4 FastAPI + SSE | ✅ |
| M0-5 Vue 前端 | ✅ |
| M0-6 端到端联调 + 模板 | ✅ |

**用户验收方式**

1. 浏览器访问 `http://<server-ip>:5173/`（同局域网或 SSH 端口转发）
2. 看到顶栏：用户 `McCoyLee` + 仓库 `McCoyLee/github-api-test@main` + 两个绿色 ✓ 徽章
3. 右侧"仓库"页签：能看到 6 个文件（含 `.github/`）
4. 右侧"密码本"页签：能添加/删除 secret
5. 右侧"运行"页签：看到 Copilot workflow（pages.yml 第一次会在 push 后出现）
6. 聊天框输入任意修改请求，观察工具调用以卡片实时流出
7. 去 GitHub 看 PR #6（M0-6 留下的示例）

**Pages 启用提醒**（一次性手动）

仓库 Settings → Pages → Source = **GitHub Actions** → 合并 PR 后 1–2 分钟 `https://mccoylee.github.io/github-api-test/` 可访问

**M0 阶段遗留 → M1 候选**

1. token 多用户：从前端传入而不是 `.env` 硬编码
2. AI 的工具集太多导致 prompt 大（17k+ input），按场景裁剪
3. PR 状态在 UI 上无法看到（要去 GitHub）——加 PR 列表 + 一键合并
4. Pages URL 没有展示到 UI——可以从 `actions/pages` API 拉
5. workflow 日志读取还没接 UI（client.py 已有 `get_run_logs`）
6. Variables 权限缺失提示
7. fine-grained PAT 权限引导文档（哪些必选哪些可选）
8. 模板从 1 个扩到 5 个（API server / cron / TG bot 等）
9. cache_hit_tokens 始终 0，加 DeepSeek prompt cache 优化

**M0 阶段开支**（DeepSeek，粗算）

- 4 次端到端 agent 调用累计 ~60k input + ~3k output tokens
- DeepSeek deepseek-chat 单价 ¥1/1M input + ¥2/1M output
- 折合约 **¥0.07** 完成全部 M0 联调

---

## M1 总体设计

用户反馈两个痛点：
1. **手动 merge 是反人性的**：小白看不懂 diff，PR 对他们无意义
2. **AI 只能小改动 + Actions 没真用上**：工具粒度小、没有反馈闭环

### M1 主线：「AI 自己跑、自己修，用户只看结果」

工作流变化：

```
M0 流程：AI 改 → 用户去 GitHub 看 diff → 手动 merge → 部署 → 看效果
M1 流程：AI 改 → 自动部署 ai/* 到预览 URL → 用户看效果
       ├ 「就这版」→ 后端 FF merge ai/* 到 main + 删分支 + 重部署 main
       └ 「再改改」→ AI 在同一 ai/* 续 commit，预览 URL 不变
```

### 用户拍板的关键决策

| 项 | 选择 | 原因 |
|---|---|---|
| 预览部署 | **GitHub Pages 单环境** | 零额外注册；缺点是同时只有一个预览 |
| 采纳策略 | **Fast-forward merge** | 保留所有 AI 的 commit，便于追溯每一步 |
| M1 节奏 | **P0+P1+P2+P3 一起做** | 4–5 周整段交付 |

### 2026-05-24 · M1-1 GitHub Pages 单预览部署 ✅

**做了什么**

- 模板 `pages.yml` 改为只接 `workflow_dispatch`（取消 push 触发，所有部署都由后端显式控制）
- `GitHubClient`：
  - `list_runs` 加 `branch` / `head_sha` 过滤
  - 新增 `get_pages_info(repo)` 查 Pages 配置（未启用返回 None）
  - 新增 `list_run_jobs` / `get_job_logs` 为 M1-3 铺路
- `backend/app/api/preview.py`：
  - `GET /api/preview?branch=...` 返回 `{pages_enabled, deployment_url, latest_run, setup_hint}`
  - `POST /api/preview/trigger?branch=...` 手动派发
- `agent.py`：
  - 跟踪 AI 修改过的 `ai/*` 分支
  - `run` 结束时若有 ai/* 改动，**自动 dispatch `pages.yml`** 触发预览
  - 新 SSE 事件 `preview_triggered` / `preview_error`
- `prompts.py` 改为 M1 预览模式版本：明确告诉 AI 不主动 open_pr、不主动 dispatch pages、续命用同分支

**验证**

```
✓ POST /api/chat 改 main.js → AI 走 read/branch/write 流程
✓ agent.run 结束自动 dispatch pages.yml on ai/preview-test-... 
✓ SSE 流出 preview_triggered 事件
✓ pages.yml 真的被触发了（虽然 conclusion=failure，因为用户还没启用 Pages）
```

**用户需做一次**：去 `https://github.com/McCoyLee/github-api-test/settings/pages`，Source 选 **GitHub Actions**。之后所有部署会真生效。

### 2026-05-24 · M1-2 一键采纳 ✅

**做了什么**

- `GitHubClient.fast_forward_merge` + `merge_branch`（FF 兜底为普通 merge）
- `backend/app/api/branches.py`：
  - `GET /api/branches` 列出 ai/* 分支
  - `POST /api/branches/{branch}/adopt`：FF merge → 失败回退 merge commit → 删 ai/* → 自动 dispatch pages 到 target
  - `POST /api/branches/discard` 丢弃 ai/* 分支
- 前端：
  - `api/client.js`：`previewApi` / `branchesApi`
  - `stores/session.js`：新增 `currentBranch` / `lastAdopted`
  - `components/PreviewPanel.vue`：iframe 预览 + 「就这版/丢弃」按钮 + 部署状态徽章 + Pages 未启用引导 + 轮询逻辑
  - `views/Workspace.vue`：右栏新增「预览」为默认 tab
  - `components/ChatPanel.vue`：从 tool_result 抽取 ai/* 分支名写入 store；处理 `preview_triggered` 事件
  - `App.vue` 顶栏：当前 ai/* 分支彩色徽章

**发现**

- 用户已经在用 M0 的 UI 自己测试！仓库里堆了 3 个 ai/* 分支（含 `ai/doudizhu-game`），都是从 main 旧版本分出来的，已偏离 → FF 失败 → 回退普通 merge 起作用了
- 烟雾测试：`POST /api/branches/discard` 删 stale 分支正常工作

**遗留**

- adopt 在 merge-commit 模式下会在 main 产生一个 merge commit，破坏「FF 干净历史」初衷；后续可加 rebase via Git Data API
- 旧 ai/* 分支（基于旧 main）只能用 merge commit 模式采纳，新分支（始终从最新 main 分出）能走 FF

### 2026-05-24 · M1-3 Actions 闭环 ✅

**做了什么**

- 静态站模板新增 `.github/workflows/ci.yml`：必需文件 + HTML 解析 + CSS 花括号配对 + JS `node --check`
- `GitHubClient`：补 `list_run_jobs` / `get_job_logs`（纯文本，不是 zip）
- 新工具 `run_ci(branch, workflow='ci.yml', timeout_seconds=180)`：
  - dispatch_workflow
  - 轮询找新建的 run（按 head_sha 匹配最准）
  - 等到 completed
  - 失败时拉每个 failed job 的步骤名 + 日志末尾 60 行
  - 返回 `{conclusion, run_id, url, failed_jobs?}`
- system prompt 升级：明确告诉 AI 写完代码必须调一次 run_ci，失败时自己看日志修

**端到端实测**

任务 1（成功路径）："改 main.js 加构建时间戳 → run_ci"
```
read → ensure_branch → write_file → run_ci → success (11s) → preview
14803 tokens
```

任务 2（失败-修复闭环）：手工造一个删了 `</body>` 的 ai/broken-XXX，让 agent 修
```
read_file × 2 (含 ci.yml) 
  → run_ci → CI#1 failure (failed_step: 'HTML 基础解析')
  → 推理出问题 
  → write_file 加回 </body>
  → run_ci → CI#2 success
  → preview
23361 tokens, 2 轮 CI
```

**关键观察**

- AI 能从 `failed_steps` 名字（来自 step name）反推问题，即使 logs_tail 内容理解吃力
- run_ci 是阻塞工具（180s 内轮询），SSE 连接稳定地保持着，前端能继续显示"运行中"状态

### 2026-05-24 · M1-4 Agent 升级 ✅

**做了什么**

- `GitHubClient.write_files`：通过 Git Data API（blob → tree → commit → ref）一次提交多文件，原子，不需要每个文件 sha
- `read_file` 新增 `start_line` / `end_line` 切片读，并始终返回 `total_lines`
- 新工具 `write_files`，schema 鼓励 AI 多文件改动时一次性调用
- system prompt 加入 plan 模式（多文件改动先 1-3 行计划再行动）

**端到端实测**

任务："加深色模式切换：style.css 加变量 + main.js 加切换逻辑 + index.html 加按钮，用 write_files 一次提交"

```
read × 3 (style.css/main.js/index.html)
  → plan：「我打算改三个文件：style.css 加 [data-theme='dark']、main.js 加切换、index.html 加按钮」
  → ensure_branch
  → write_files(3 files) → commit a14c9c2
  → run_ci → success
  → preview
18714 tokens
```

对比之前同复杂度任务用 56k+ tokens（反复纠结、多次 write_file），效率提升 ~3 倍。

**未做**

- tree-sitter 项目索引：留 M2，目前仓库不大不阻塞
- DeepSeek prompt cache 命中观测：OpenAI SDK 的 `usage.model_dump()` 不带 `prompt_cache_hit_tokens` extra 字段；缓存本身是 DeepSeek 自动的，看不到具体命中率但不影响功能

### 2026-05-24 · M1-5 模板扩展 ✅

**做了什么**

新增两个完整模板：

**api-server**（FastAPI 后端）
- `app/main.py` + `tests/test_main.py`（pytest）+ `requirements.txt`
- `.github/workflows/ci.yml`：ruff + pytest
- `.github/workflows/deploy.yml`：push main → Render Deploy Hook
- `AGENTS.md`：约束 AI 加路由必须配测试 / 加依赖更新 requirements
- `template.json`：声明需要 `RENDER_DEPLOY_HOOK` secret

**cron-task**（定时任务 + 邮件）
- `src/job.py`（业务入口）+ `tests/test_job.py`
- `.github/workflows/ci.yml`：pytest + 入口可执行性检查
- `.github/workflows/cron.yml`：每天北京 7:00 跑 + workflow_dispatch
- `AGENTS.md`：约束 AI 只改 `do_job()`、改 cron 时间用 UTC
- `template.json`：声明 `SMTP_USER` / `SMTP_PASS` / `MAIL_TO` secrets

**后端**

- `backend/app/api/templates.py`：`GET /api/templates` 列出 + `GET /api/templates/{id}` 详情
- 每个模板的 `template.json` 含：id / name / description / tags / required_secrets / deploy_target
- 自动统计 `file_count` 和 `primary_files`

**脚本**

- `scripts/init_template.py` 接受模板名参数：`python scripts/init_template.py api-server`

**前端**

- 右栏新增「模板」tab，`TemplatesPanel.vue` 卡片展示 3 个模板，含 required_secrets 列表
- 验证：`GET /api/templates` 正确返回 3 个模板的完整 metadata

**遗留 → M2**

- "新建项目"完整工作流：让用户选模板 → 后端按模板自动创建 GitHub 仓库 + 推内容；当前必须手工跑 `init_template.py`

### 2026-05-24 · M1-6 多用户凭据 ✅

**做了什么**

- 后端 `backend/app/api/deps.py`：依赖改成 header 优先 + `.env` 兜底
  - `X-GitHub-Token` → 用户的 GitHub PAT
  - `X-AI-Provider` + `X-AI-Key` + `X-AI-Model` → 用户的 AI 凭据
  - `X-Repo` → 用户选的仓库（query param > header > .env）
- `_provider_singleton(provider, key, ...)` 按 (provider, key) 复用 client，避免每请求新建
- 前端：
  - `stores/vault.js`：localStorage 存凭据 + `authHeaders()` 方法
  - `api/client.js`：axios 拦截器自动注入凭据头
  - `api/chat.js`：fetch SSE 也注入头
  - `components/VaultDialog.vue`：凭据配置对话框（GitHub token、AI provider、AI key、AI model、repo）
  - `App.vue`：首次启动若 health 显示后端没凭据且本地 vault 也空 → 自动弹凭据对话框；顶栏加齿轮按钮可随时打开

**实测**

```
curl -H "X-GitHub-Token: ..." -H "X-Repo: McCoyLee/github-api-test" -H "X-AI-Key: ..." \
     /api/repo/info → 200 OK，user=McCoyLee
GET /api/templates → 3 个模板（无需凭据，纯文件元数据）
```

**安全性提醒**

- localStorage **明文存储**凭据（M1 简化版）
- XSS 风险存在；M2 加 WebCrypto + 口令解锁的保险箱机制
- 当前不支持多 repo 切换（一个仓库一个会话），M2 加 repo 切换器

---

## M1 总结

| 任务 | 状态 | 关键产出 |
|---|---|---|
| M1-1 预览部署 | ✅ | pages.yml dispatch-only + agent 自动派发 + `/api/preview` |
| M1-2 一键采纳 | ✅ | FF merge（带 merge-commit 兜底）+ 预览 iframe + 「就这版/丢弃」按钮 |
| M1-3 Actions 闭环 | ✅ | ci.yml + `run_ci` 工具 + 失败-修复 2 轮迭代实测通过 |
| M1-4 Agent 升级 | ✅ | `write_files` 原子多文件 + `read_file` 行切片 + plan 模式，token 降 60% |
| M1-5 模板生态 | ✅ | 3 个模板（static-site / api-server / cron-task）+ `/api/templates` |
| M1-6 多用户凭据 | ✅ | header 注入 + 前端 localStorage 保险箱 + 凭据对话框 |

**质量**

- 后端 11 个端点 + 1 SSE 流，无业务数据库
- AI 工具集 11 个（增加 write_files + run_ci）
- 前端 5 个面板：聊天 / 预览 / 仓库 / 密码本 / 运行 / 模板
- 端到端验证场景 5 个（创建/修改/批量/CI 失败修复/采纳）

**M1 开销**（DeepSeek）

- 4 次大型 e2e ≈ 95k tokens ≈ **¥0.11**
- 全部 M1 联调约 **¥0.20**

**M2 候选清单**

1. WebCrypto 加密的凭据保险箱 + 口令解锁
2. 仓库切换器（用户管理多个项目）
3. "新建项目"完整流：选模板 → 创建 GitHub 仓库 → 推模板
4. tree-sitter 项目索引（大项目 cheat sheet）
5. ai/* 分支基于最新 main 的 rebase（让 FF 永远成功）
6. PR 列表 + 评论（多人协作）
7. workflow 日志实时流式（不只是 tail，是 live tail）
8. 移动端响应式
9. Tauri 打包成桌面应用
10. 多用户 SaaS（统计、配额、付费）

---

## M2 主题 D：「能分享」+「AI 能干大活」 ✅

用户拍板：A+B 一起做，不分拆。8 个任务全部完成。

### 2026-05-25 · M2-1 后端「新建项目」API ✅

- `GitHubClient.create_repo` / `enable_pages` / `open_environment_branches`
- `POST /api/projects/create`：创建仓库 + 推模板 + 启用 Pages + 放开分支策略（一站式）
- `POST /api/projects/init`：把模板推到**已经手动建好**的空仓库（避免 fine-grained PAT 创建权限限制）
- `POST /api/projects/create-stream`：SSE 流式进度版（备用）
- 实测：/init 7 文件推送 + Pages 启用成功；/create 因 PAT 缺 Repository creation 权限报 403，错误信息引导清晰

### 2026-05-25 · M2-2 前端「新建项目」向导 ✅

- `NewProjectDialog.vue`：3 步引导（选模板 → 填信息 → 实时进度）
- 两种创建模式可选：自动建仓库 vs 推到已有空仓库
- 必需 secrets 自动提示
- 完成后【切到这个项目】按钮直接更新 vault + bootstrap 切换会话

### 2026-05-25 · M2-3 多仓库切换器 ✅

- `vault.recentRepos`：最近用过的 N 个仓库（LRU 10 个）
- `vault.switchRepo(repo)`：切换 + 自动加入 recent 列表
- `RepoSwitcher.vue`：顶栏 dropdown，列最近仓库 + 手动加新仓库 + 删除
- 替代顶栏静态仓库 tag；切换时 sess.reset() 重新加载

### 2026-05-25 · M2-4 首次启动引导优化 ✅

- 新端点：
  - `GET /api/probe/github`：用 header token 试调 /user 验证
  - `GET /api/probe/github/repos`：列 PAT 能访问的仓库（用于下拉）
  - `POST /api/probe/ai`：极小请求验 AI key
- `VaultDialog.vue` 升级：
  - 每个凭据字段右侧【测试】按钮，实时显示 ✓/✗ + 错误原因
  - 仓库字段改成 `el-select` filterable allow-create，自动列出能访问的仓库
- 实测：GitHub probe 返回 McCoyLee；AI probe 9 tokens 返回"好"；列出 5 个 repos

### 2026-05-25 · M2-5 项目索引工具 ✅

- `backend/app/ai/outline.py`：纯 Python 实现（不引 tree-sitter 避免装系统依赖）
  - `.py` 用 stdlib `ast`（准确）
  - `.js/.ts/.jsx/.tsx/.mjs` 正则匹配 function / class / arrow
  - `.vue` 提取 `<script>` 块当 JS 处理
  - `.html` 提取 `<title>` + 主要 section
  - `.md` 提取标题层级
- 新工具 `get_project_outline(ref?, max_files=40)`：
  - 通过 Git Trees API `recursive=1` 一次拿全树
  - 对代码文件抽符号
  - 返回 markdown 渲染版给 AI（省 token）
- 实测仓库：输出 7 文件 + 标题/section 全提取正确

### 2026-05-25 · M2-6 大文件强制切片读 ✅

- `_read_file` 加阈值 `LARGE_FILE_LINES = 300`
- 文件 > 300 行且未传 start_line/end_line：自动返回前 30 + 后 20 行 + hint，提示 AI 用行号切片
- 用户传 start_line/end_line：精确切片返回
- 防 AI 在大文件上"复制原文"出错

### 2026-05-25 · M2-7 计划模式 UI ✅

- 新工具 `propose_plan(steps, rationale?)`：AI 大改动前提议计划
- system prompt 升级：明确"大改动（>3 文件 或 多步骤）先 propose_plan，停下来等用户确认"
- `PlanCard.vue`：渲染 plan 为 checklist，可勾选/取消
- ChatPanel 识别 propose_plan 工具结果，特殊渲染为 PlanCard
- 用户点【按此执行】→ 自动发新消息"已确认计划，请按以下顺序执行：..."

### 2026-05-25 · M2-8 workflow 日志实时流 ✅

- `GET /api/runs/{run_id}/stream` SSE：
  - 初始推 `status` 事件
  - 轮询 `list_run_jobs` + `get_job_logs`（每 2s）
  - 维护 per-job 字节偏移量，只推**增量** chunk
  - run 完成时推 `done` 后流终止
  - 10 分钟超时保护
- `LogStreamDialog.vue`：暗色终端样式，按 job 分块显示，自动滚到底
- RunsPanel 每行加【看日志】按钮
- 实测对一个 completed run：status + done 正常推送

---

## M2 总结

| 任务 | 状态 | 关键产出 |
|---|---|---|
| M2-1 新建项目 API | ✅ | /api/projects/{create,init,create-stream} |
| M2-2 新建项目向导 | ✅ | 3 步引导式 dialog + 实时进度 |
| M2-3 多仓库切换 | ✅ | vault 存 LRU + 顶栏 dropdown |
| M2-4 启动引导优化 | ✅ | 3 个 /api/probe/* + 凭据测试按钮 + 仓库下拉 |
| M2-5 项目索引 | ✅ | outline.py (5 语言) + get_project_outline 工具 |
| M2-6 大文件切片 | ✅ | LARGE_FILE_LINES=300 自动截断 + hint |
| M2-7 计划模式 | ✅ | propose_plan 工具 + PlanCard checklist UI |
| M2-8 日志实时流 | ✅ | /api/runs/{id}/stream SSE 增量推送 + LogStreamDialog |

**质量数字**

- 后端端点从 11 → 19 个
- AI 工具集从 11 → 13 个（加 propose_plan, get_project_outline）
- 前端面板从 6 → 9 个组件（加 NewProjectDialog, RepoSwitcher, PlanCard, LogStreamDialog）

**关键体验提升**

| 维度 | M1 | M2 |
|---|---|---|
| 新用户上手 | 命令行跑 init_template.py | UI 点【新建项目】3 步 |
| 多项目管理 | .env 改 TEST_REPO | 顶栏 dropdown 切换 |
| 凭据填写 | 盲填没验证 | 实时测试 ✓/✗ + 仓库下拉自动列出 |
| AI 了解大项目 | 反复 list_dir + read_file | 一次 get_project_outline 看全图 |
| 大改动 | AI 自己琢磨 | 列计划用户勾选确认 |
| 看 workflow 日志 | 跳到 GitHub 网页 | UI 内实时流 |
| 大文件改动 | AI 复制原文易错 | 自动切片 + 强制行号读 |

**M2 开销**（DeepSeek + 服务器时间）

- M2 联调累计 ~20k tokens ≈ **¥0.03**
- M2 总共约 **¥0.10**（含错误重试）

**M3 候选方向**

1. WebCrypto 凭据保险箱 + 口令解锁（M1/M2 都没做）
2. ai/* 自动 rebase 到最新 main（让 FF 永远成功）
3. Tauri 桌面端打包（Windows/Mac 双击安装）
4. 模板生态扩展（已有 3 个 → 5-8 个，含 TG bot / 数据流水线 / Flask 等）
5. PR 列表 + 多人协作
6. 移动端响应式
7. 真正的多用户 SaaS（用户隔离 + 配额 + 计费）

---

### M1 任务清单

- **M1-1** GitHub Pages 单预览部署：模板 pages.yml 改 dispatch-only + 后端 `/api/preview` + agent 写完自动触发部署
- **M1-2** 一键采纳：`/api/branches/{branch}/adopt` FF merge + 前端预览 iframe + 「就这版/再改改」按钮
- **M1-3** Actions 闭环：ci.yml + agent 自动跑测试 + 失败拉日志回喂 AI 修复（≤3 轮）
- **M1-4** Agent 升级：多步计划、`write_files` 批量、`read_file` 行范围、DeepSeek prompt cache、可选 tree-sitter 索引
- **M1-5** 模板生态：api-server (FastAPI+Render)、cron-task (定时任务+邮件)；新建项目模板选择 UI
- **M1-6** 多用户凭据：脱离 `.env` 硬编码，前端 localStorage 存 token；后端读 header

### 关键设计要点

1. **PR 隐式化**：仍然建 PR（GitHub 协作记录），但 UI 全程不出现 "PR" 字样；用户只看预览和按钮
2. **预览触发**：agent.run 结束时若 ai/* 有新 commit → 自动 `dispatch_workflow('pages.yml', ref=ai/*)`
3. **Actions 闭环**：write → dispatch_ci → wait_for_run → 失败时拉 logs 关键段 → 续命修 → 最多 N 轮
4. **大项目支撑**：tree-sitter 抽符号表喂 AI 当 cheat sheet，避免每次塞全文
5. **token 成本**：DeepSeek prompt caching（system + tools + AGENTS.md 锁头部）

---

## M3 主题 C：「产品化」+「Tauri 桌面端」 ✅

用户拍板：A（产品化 3 项）+ B（Tauri）一起做。6 个任务全部完成。

### 2026-05-25 · M3-1 WebCrypto 凭据加密保险箱 ✅

- `frontend/src/utils/crypto.js`：PBKDF2(250k, SHA-256) 派生 key + AES-GCM 加解密
- `vault.js` 重构：明文 / 加密两种模式
  - `locked` 状态：检测到密文且未解锁时，authHeaders 返回空、阻止读写
  - `unlock(pwd)` / `enableEncryption(pwd)` / `disableEncryption()` / `changePassword(pwd)`
  - 所有写操作改 async（persist 时按是否有 password 决定加密/明文）
- `UnlockDialog.vue`：启动时若检测到加密 vault，强制解锁（含「忘了/重置」逃生口）
- `VaultDialog.vue`：加「启用主密码加密」开关 + 密码设置/修改
- `App.vue`：bootstrap 前检查 locked → 先弹解锁；顶栏徽章显示 🔒已锁定 / 🔐已加密 / 📝明文
- 忘记密码无法恢复（AES-GCM 特性），只能清空重填——已在 UI 说明

### 2026-05-25 · M3-2 ai/* 自动 rebase ✅

- `GitHubClient.compare_refs` / `squash_rebase_branch`
  - 用 compare API 拿 ai/* vs main 的 changed files
  - 以 main HEAD tree 为 base_tree，把 ai/* 的文件内容做成新 blob/tree/commit
  - force-update ai/* 指向新 commit → 之后 FF 一定成功
- adopt 端点策略升级：FF 优先 → 失败则 squash-rebase + FF → 再失败兜底 merge-commit
- 实测 stale 分支 `ai/ci-loop`：diverged(ahead=1,behind=2) → rebase → ahead(behind=0) 可 FF ✓
- 代价：squash 丢中间 commit 历史，但 main 保持线性、对小白更清晰

### 2026-05-25 · M3-3 模板生态扩 3 个 ✅

模板从 3 → **6** 个：

- **telegram-bot**：python-telegram-bot，polling 调试 / webhook 部署 Render；secret: TELEGRAM_BOT_TOKEN
- **flask-app**：Flask + gunicorn + pytest + Procfile，部署 Render
- **data-pipeline**：fetch/transform/save 三段式，cron 跑 + commit 回 data/ + artifact
- 每个含 template.json / AGENTS.md / README / ci.yml + 业务代码
- `GET /api/templates` 验证返回 6 个，metadata 完整

### 2026-05-25 · M3-4 + M3-5 Tauri 桌面壳 + sidecar ✅（代码就位，本机无法编译）

- `src-tauri/`：Tauri 2 项目骨架
  - `tauri.conf.json`：frontendDist 指 frontend/dist，beforeBuildCommand 跑前端 build
  - `Cargo.toml` / `build.rs` / `main.rs`
  - `src/lib.rs`：**启动 spawn Python 后端**（`python -m uvicorn ...`）+ 退出 kill；环境变量可配 `AFO_BACKEND_CMD/ARGS/CWD/SKIP_BACKEND`
  - `capabilities/default.json`：Tauri 2 权限
  - `package.json`：tauri cli scripts
- 配置文件 JSON/TOML 语法全部校验通过
- **重要限制**：本服务器无 Rust + 无 webkit2gtk + 无头，**无法 build/运行**；源码为本地（Mac/Win/Linux 桌面）构建准备

### 2026-05-25 · M3-6 桌面打包文档 + 收尾 ✅

- `docs/desktop.md`：完整本地构建指南
  - 前置依赖（Rust/Node/平台系统库/Python）
  - 图标生成（tauri icon）
  - dev / build 流程
  - **后端两种打包策略**：A) 要求用户有 Python（默认）；B) PyInstaller 冻结成 sidecar 二进制（含 PyNaCl `--collect-all nacl` 注意事项）
  - 常见问题
- `scripts/dev.sh`：一键起后端 + 前端的开发脚本

---

## M3 总结

| 任务 | 状态 | 关键产出 |
|---|---|---|
| M3-1 凭据加密 | ✅ | WebCrypto AES-GCM 保险箱 + 解锁流程 |
| M3-2 自动 rebase | ✅ | squash_rebase_branch，FF 永远成功 |
| M3-3 模板扩展 | ✅ | 3→6 个模板（TG bot/Flask/数据流水线） |
| M3-4 Tauri 壳 | ✅* | src-tauri 完整骨架（本机不能编译） |
| M3-5 sidecar | ✅* | lib.rs 后端进程生命周期管理 |
| M3-6 文档 | ✅ | docs/desktop.md + dev.sh |

\* 代码就位，需在桌面开发机本地编译验证

**综合 e2e（验证 M2+M3 新工具链）**

任务："看项目结构 → 大改加导航/关于/联系页 → 先提计划"
```
get_project_outline (7 文件)
  → read_file × 3
  → propose_plan (6 步)
  → "我提了个计划，确认后开干 👆"（停下等用户）
15205 tokens
```
get_project_outline ✓、propose_plan ✓、plan 后正确停止 ✓

**质量数字（M3 末）**

- AI 工具集 13 个（M3 未增工具，propose_plan/get_project_outline 是 M2 加的）
- 后端 GitHubClient 方法新增：compare_refs / squash_rebase_branch / create_repo / enable_pages / open_environment_branches
- 前端组件：新增 UnlockDialog；vault.js 全面 async 化 + 加密
- 模板：6 个
- 新增 src-tauri/（8 文件）+ docs/desktop.md + scripts/dev.sh

**M3 限制与遗留**

1. Tauri 本机未编译（无 Rust/webkit/GUI）——需用户本地构建
2. PyInstaller sidecar 未实测（文档化了流程）
3. squash-rebase 丢中间 commit 历史（设计权衡）
4. 凭据加密：忘密码不可恢复（AES-GCM 特性）

**M4 候选**

1. 在桌面机上真编译 Tauri + PyInstaller sidecar 跑通"双击即用"
2. PR 列表 + 多人协作 ← **M4-1 已做（见下）**
3. 移动端响应式 / PWA
4. 多用户 SaaS（隔离 + 配额 + 计费）
5. 国内 GitHub 加速反代内置

---

## M4

### 2026-05-31 · M4-1 PR 协作工作流 ✅

把「采纳 AI 分支」从单一的「就这版直接 FF 进 main」升级成**可选的 PR 协作路径**：想快还是直接 adopt，想协作/留痕/让别人 review 就走 PR。两条路并存。

**后端**

- `GitHubClient` 新增方法：`list_prs`（按更新倒序，支持 head/base 过滤 + 自动补 `owner:` 前缀）/ `get_pr` / `list_pr_files` / `list_pr_comments` / `create_pr_comment` / `get_combined_status`（legacy status API）/ `list_check_runs`（Actions 走这个）；`open_pr` 加 `draft`，`merge_pr` 加 `commit_title`
- 新路由 `api/prs.py`（6 端点，挂在 `/api/prs`）：
  - `GET /api/prs?state=open|closed|all` 列表
  - `GET /api/prs/{number}` 详情：基本信息 + 改动文件 + **CI 状态汇总**（status + check-runs 揉成 overall: success/failure/pending/none）+ 评论
  - `POST /api/prs` 从 head 分支开 PR（已有 open PR 则复用不重开，422 友好报错）
  - `POST /api/prs/{number}/comment` 留言
  - `POST /api/prs/{number}/merge` 合并（默认 squash，可顺带删 ai/* 分支 + dispatch pages.yml 刷新预览；405/409 友好报错）
  - `POST /api/prs/{number}/close` 关闭不合并

**前端**

- `api/client.js` 加 `prsApi`（list/get/create/comment/merge/close）
- 新组件 `PrPanel.vue`：Workspace 加「协作」标签页
  - open/closed/all 切换 + 列表（点行进抽屉详情）
  - 顶部「选 ai/* 分支 + 标题 → 提 PR」
  - 详情抽屉：状态/CI 标签、改动文件（A/M/D 着色 + 增删行数）、CI 检查列表、讨论区 + 发评论、**合并/关闭**按钮
- `PreviewPanel.vue` 在「就这版/丢弃」下方加一行「🔀 想让别人先看看？提个 PR」——从主决策点直达协作流

**测试**

- `scripts/smoke_pr.py`：建分支→写文件→开 PR→列表→详情(文件/CI/评论)→留言→squash 合并→验证分支清理。真实仓库跑通 ✅（PR #11，merged=True）
- HTTP 层验证：`GET /api/prs`、`GET /api/prs/{n}`（CI overall=success）、404 友好报错均通过
- 前端 `npm run build` 通过

**铁律遵循**：无新增 DB；凭据走 header；合并默认删 ai/* 分支保持 main 稳定；新端点遵循既有 deps + 错误处理风格。

**M4-1 限制**

1. 详情每次拉 5 个 GitHub 请求（pr/files/status/check-runs/comments），未并发未缓存——单 PR 够用，列表页不拉详情所以列表不慢
2. 跨 fork PR 未特别处理（head 默认同仓库 owner）
3. PR review（行内 comment / approve）未做，只做了 issue-comment 级讨论

### 2026-05-31 · M4-2 PWA + 移动端响应式 ✅

让浏览器应用「可安装」（加到手机主屏/桌面）+ 窄屏不再挤成一团。**纯前端、零新依赖**（手写 SW/manifest，避免本机不可靠的 npm install）。

**PWA（可安装 + 离线外壳）**

- `public/manifest.webmanifest`：name/short_name/icons/standalone/theme_color，满足安装条件
- `public/icon.svg`：矢量 app 图标（深色芯片 + AI 字样，`purpose: any maskable`）
- `public/sw.js`：手写 service worker
  - 导航请求 network-first，断网回退缓存的 index.html（不白屏）
  - 静态资源 stale-while-revalidate
  - `/api` 和 `/healthz` 永远直连、绝不缓存（数据真相在用户 GitHub）
  - 产物文件名带 hash，故运行时按需缓存，不维护预缓存清单
- `index.html`：加 manifest/theme-color/apple-touch-icon/mobile-web-app-capable
- `main.js`：仅生产构建注册 SW（`import.meta.env.PROD`），dev 不注册

**响应式**

- `Workspace.vue`：`@media (max-width: 860px)` 左右两栏 → 上下堆叠，整页可滚（聊天在上、预览/协作等标签在下，各给 min-height 保证可用）
- `App.vue`：顶栏 `flex-wrap` + 收紧间距，窄屏按钮不再被挤出屏幕；`.main` 加 overflow auto

**验证**

- `npm run build` 通过，dist 根正确产出 manifest.webmanifest / sw.js / icon.svg，index.html 引用齐全
- 本地 http.server 起 dist：manifest（application/manifest+json）/ sw.js（text/javascript）/ icon.svg（image/svg+xml）均 200 且 MIME 正确
- manifest JSON / icon SVG / sw.js 语法校验通过

**M4-2 限制**

1. SW 离线仅缓存外壳；应用本质在线（要连 GitHub + AI），离线只能开壳不能干活
2. 图标只给了 SVG（现代浏览器够用）；老 iOS/安卓如需 PNG 多尺寸要 `tauri icon`/在线工具补
3. 复杂双栏工作台在很窄手机上仍偏挤，仅做到「能用」非「完美移动体验」

### 2026-05-31 · M4-3 PR 文件 diff 预览 ✅

M4-1 的 PR 详情只列了文件名 + 增删行数，**看不到具体改了啥**，没法真 review。补上 unified diff。

- 后端 `prs.py`：`get_pr` 的 files 带上 GitHub files API 的 `patch`；`_truncate_patch` 超过 2 万字截断（防前端卡死），二进制文件 patch 为 None
- 前端 `PrPanel.vue`：每个文件行可点击展开/收起，渲染 GitHub 风格深色 diff —— `@@` hunk 灰、`+` 绿、`-` 红；展开状态随文件名记在 `openFiles` Set，切 PR 时重置
- 验证：HTTP 拉 PR #10，`files[0].patch == "@@ -0,0 +1 @@\n+sse pipe ok"`，渲染分类正确；build 通过

至此 PR 协作闭环完整：列表 → 详情(看 diff + CI + 讨论) → 留言 → 合并/关闭。
