# AI 造应用 · 开发大纲（小到玩具大到正经项目都能做）

## 一、定位

不再只是"小工具生成器"。**目标范围**：

- 小：单页网页、记账本、个人主页
- 中：带后端的小程序、API 服务、爬虫、定时任务、Telegram/微信机器人
- 大：多模块项目、有数据库、有部署流水线、有定时跑批

**面向人群**：中国大陆、有想法但没编程基础的人。

**核心抓手**：把 **GitHub 仓库 + GitHub Actions + GitHub Secrets** 三件套包装成小白能用的"应用项目"概念——

- 仓库 = 你的项目
- Actions = 项目的"运行按钮"和"自动化"
- Secrets = 项目的"密码本"（API Key、数据库密码、Token）

用户在客户端里点几下，背后全是仓库操作。

**形态**：桌面客户端（Windows/Mac 双击安装），不是网页。打开就能用，不用部署、不用域名。

---

## 二、为什么是 GitHub（而不是 Gitee）

| 能力 | Gitee | GitHub |
|---|---|---|
| Actions 免费额度 | 限制多、私有仓基本没有 | 公开仓无限，私有仓每月 2000 分钟 |
| Secrets / Variables | 弱 | 成熟，支持环境维度 |
| 生态（部署 / 镜像 / 第三方集成） | 少 | 海量 marketplace |
| 国内访问 | 快 | 慢，但可解决 |

**结论**：核心走 GitHub。访问问题客户端层面解决（见第六节）。

---

## 三、用户视角的关键概念翻译

小白看到的 → 背后做的：

| 用户看到 | 背后 |
|---|---|
| 「新建项目」 | 从模板 fork 一个 GitHub 仓库 |
| 「保存这一版」 | git commit + push |
| 「运行 / 部署」 | 触发 workflow_dispatch |
| 「定时跑」 | 写一条 cron workflow |
| 「我的密码本」 | GitHub Secrets 管理 UI |
| 「环境配置」 | GitHub Variables 管理 UI |
| 「运行日志」 | 拉 Actions logs，SSE 渲染 |
| 「上线 / 发布」 | tag + release workflow，或 Pages / Vercel deploy |
| 「项目状态」 | 最新 workflow run 的颜色徽章 |

---

## 四、核心功能模块

### 1. 项目类型与模板（决定生态广度）

**模板按"运行形态"分类**，每个模板自带一套 `.github/workflows/`：

| 形态 | 例子 | Actions 干什么 |
|---|---|---|
| 静态网页 | 个人主页、单页应用 | 构建 + 推 Pages |
| 后端 API | Flask/FastAPI/Node 服务 | 构建镜像 + 部署到 Fly.io/Render/自家 VPS |
| 定时任务 | 每天爬豆瓣、每周发周报 | cron workflow，跑脚本，结果发邮件/钉钉 |
| 机器人 | Telegram bot、微信机器人 | 长驻部署 或 webhook + serverless |
| 数据流水线 | 抓数据 → 处理 → 存库 | 多 step workflow，artifact 存中间产物 |
| 桌面/移动 App | Tauri / Flutter 项目 | matrix build 多平台产物，release 发版 |

每个模板包含：

- 可跑通的代码骨架
- 预置 workflows
- 给 AI 看的 `AGENTS.md`（项目结构 + 改动约定 + 测试命令）
- 给用户看的 `README.md`（说人话）

### 2. AI 改代码引擎（撑得起大项目）

小项目改几个文件就够，大项目得有真本事：

- **工具集**（基于 GitHub API，全程不用本地 clone）：
  - `read_file` / `write_file` / `list_dir` / `grep_code`（GitHub Search API）
  - `read_workflow` / `update_workflow` / `dispatch_workflow`
  - `read_run_logs`（看 Actions 输出）
  - `read_secrets_names`（只读名字，值看不到）/ `set_secret` / `set_variable`
  - `create_branch` / `open_pr` / `merge_pr`
  - `query_issues` / `add_comment`（让用户 issue 反馈也能进入 agent 循环）

- **大项目特别处理**：
  - **上下文压缩**：仓库太大塞不进 prompt，先用代码符号索引（ctags / tree-sitter）摘要 + 按需 `read_file`
  - **分支隔离**：AI 永远在 `ai/xxx` 分支干活，不污染 main
  - **任务拆分**：用户说"重构整个用户系统"→ AI 先列计划 → 用户勾选 → 一步步执行，每步一个 commit
  - **跑测试反馈环**：写完代码自动 dispatch test workflow → 失败日志回喂 AI → 最多 N 轮自修复

### 3. Secrets / Variables 管理 UI（关键差异点）

这是把"小白也能搞大项目"变成可能的核心模块。

界面：

```
项目 → 密码本
┌────────────────────────────────────────┐
│  + 添加密码                            │
│                                        │
│  🔒 OPENAI_API_KEY      ··········  ✏ │
│  🔒 DATABASE_URL        ··········  ✏ │
│  🔒 TELEGRAM_BOT_TOKEN  ··········  ✏ │
│                                        │
│  📝 环境变量（非敏感）                 │
│  ENV = production                      │
│  REGION = cn-shanghai                  │
└────────────────────────────────────────┘
```

- 新建 secret 时引导："这个值会加密存在 GitHub，连 AI 都看不到"
- AI 在写代码时**只能知道有哪些 secret 名字**，引用方式自动用 `${{ secrets.XXX }}`
- 模板里常用的 secret 有"推荐清单"：选模板"Telegram 机器人"→ 自动提示需要 `TELEGRAM_BOT_TOKEN`，一键填入
- 支持**环境分组**（dev / prod）→ 对应 GitHub Environments

### 4. 运行与部署（一键化）

主界面顶部的"运行"按钮，按项目类型变形：

- 静态站 → "发布"按钮 → 触发 Pages workflow → 出来一个 `xxx.github.io/yyy` 链接（国内访问慢，自动同时镜像到 Gitee Pages 或者 Cloudflare）
- 后端服务 → "部署"按钮 → 先问"部到哪？"（内置选项：Render / Fly.io / Railway / 你自己的 VPS via SSH）→ 把对应 secrets 填上 → dispatch deploy workflow
- 定时任务 → "立即跑一次" / "查看下次运行时间"
- 长驻服务 → 显示在线状态（拉部署平台的 health check）

### 5. 日志与可观测

- Actions 日志实时流式拉取（GitHub 提供日志 API + Webhook）
- AI 看得懂错误：从日志中抽取关键错误段，触发"要不要让 AI 帮你修？"按钮
- 长跑任务的进度：跑批 workflow 把进度写到 GitHub Issue 或 artifact，客户端轮询

### 6. 协作（大项目早晚要的）

- 把队友拉进 GitHub 仓库的 collaborator
- 客户端里也能看 issue / PR，让 AI 处理 issue（"帮我看看 #42 该怎么解决"）
- 不自建任何后端，全靠 GitHub 原生协作

### 7. 成本面板

小白最怕"用着用着钱没了"：

- 顶部一直显示"今天花了 ¥0.3 / 余额 ¥9.7"
- 单次对话超过 5 毛钱弹窗确认
- 提供"省钱模式"：用更便宜的模型 + 更短上下文

---

## 五、典型工作流示例

**例：用户想做一个"每天早上 7 点抓 A 股新闻，整理后发我邮箱"的服务**

1. 用户：新建项目 → 选模板"定时任务 + 邮件通知"
2. 客户端 fork 模板仓库，自动建好 `cron.yml`（每天 7 点）+ `send.yml`
3. 提示用户填密码本：`SMTP_USER` / `SMTP_PASS`（带教程链接：怎么开 QQ 邮箱授权码）
4. 用户对 AI 说："抓东方财富新闻，只要科技板块，整理成 markdown 表格发我"
5. AI 列计划 → 写 `fetch.py` → 提交到 `ai/news-task` 分支
6. AI 自动 dispatch "测试运行" workflow
7. 失败：CSS 选择器错了 → AI 看日志 → 改 → 再跑 → 成功，邮箱收到样本
8. 用户点"满意，正式启用" → 合并到 main → 定时任务上线
9. 之后每天 7 点自动跑，跑挂了客户端有红点提示，AI 主动给修复建议

整个过程用户没打开过浏览器、没碰过 YAML、没碰过 Python。

---

## 六、解决 GitHub 国内访问的实操方案

这是不能回避的工程问题：

1. **API 走加速域名**：客户端配置可切换 `api.github.com` 的反代（自建 Cloudflare Worker，或社区现成的镜像）
2. **Actions 触发**：用 API dispatch，不依赖网页，反代后稳定
3. **Pages 访问**：自动配 Cloudflare Pages 或 Gitee Pages 同步，给用户一个国内能开的链接
4. **OAuth 登录**：device flow（用户去网页输 8 位码，无回调），避免在国内打不开的 callback URL
5. **首次安装**：附带"网络体检"页面，自动测速并切到最快线路
6. **兜底**：所有功能都允许用户手动填 PAT，不强依赖 OAuth

---

## 七、技术选型

| 模块 | 选型 | 说明 |
|---|---|---|
| 客户端框架 | **Tauri 2** + Vue 3 + Element Plus | 小、快、中文友好 |
| GitHub SDK | Octokit（REST + GraphQL） | 大项目要 GraphQL 减少 API 次数 |
| 代码索引 | tree-sitter（Rust 端） | 大仓库符号摘要 |
| 本地缓存 | SQLite + 文件系统 LRU | 缓存 blob/tree，省 API 配额 |
| AI Provider | DeepSeek 默认 / Claude / 通义千问 / Kimi / GPT | 工具调用全部走标准 function calling |
| 网络层 | 内置可切换代理 | 解决 GitHub 访问 |
| 日志展示 | xterm.js（伪终端样式） | 跟 Actions 网页观感一致 |

---

## 八、最小目录结构

```
ai-maker/
├── src-tauri/                # Rust 壳
│   └── src/
│       ├── main.rs           # 启动入口
│       ├── git.rs            # Git/GitHub API 封装
│       ├── proxy.rs          # 网络加速层
│       └── indexer.rs        # tree-sitter 代码索引
├── src/                      # Vue 前端
│   ├── views/
│   │   ├── Onboarding.vue    # 引导（填 Key、登录 GitHub）
│   │   ├── Workspace.vue     # 主界面（聊天 + 预览）
│   │   ├── Secrets.vue       # 密码本管理
│   │   ├── Runs.vue          # Actions 运行日志
│   │   └── Deploy.vue        # 部署面板
│   ├── ai/
│   │   ├── providers/        # deepseek.ts / qwen.ts / kimi.ts / claude.ts
│   │   ├── tools.ts          # 工具集 schema
│   │   └── agent.ts          # 改代码循环
│   └── templates/            # 内置模板清单
│       ├── static-site/
│       ├── api-server/
│       ├── cron-task/
│       ├── tg-bot/
│       └── ...
├── installers/               # 打包脚本（Windows/Mac/Linux）
└── docs/                     # 用户文档
```

---

## 九、分阶段路线

**M0 · 跑通骨架（3 周）**

- Tauri 壳 + GitHub OAuth device flow
- 单模板（静态网页）+ AI 改代码 + push + Actions dispatch + 日志回看
- DeepSeek 接入

**M1 · 撑起中等项目（4 周）**

- Secrets / Variables 管理 UI
- 5 个模板（含后端 API、定时任务）
- 分支隔离 + PR 合并 UI（说人话版）
- 失败日志自动回喂

**M2 · 大项目能力（4 周）**

- 代码符号索引 + 智能上下文
- 多步任务规划
- 环境（dev/prod）切换
- 部署到 Render / Fly.io / VPS

**M3 · 生态与稳定性（持续）**

- 模板市场（GitHub topic 检索）
- 网络加速 / 镜像优化
- 协作、issue agent
- 成本与配额面板

---

## 十、给用户的承诺

1. 不用敲代码
2. 不用碰 Git、YAML、命令行
3. 不用买服务器（前期用免费额度就够）
4. 小项目 10 块钱玩一个月，大项目按需买 AI Token
5. 项目永远是你的，仓库归你，随时能带走

---

## 十一、下一步可推进的方向

- **(a)** 把 AI 的工具集 schema + system prompt 写细（核心引擎）
- **(b)** 设计 Secrets/Variables 管理 UI 的具体交互稿
- **(c)** 写一个完整模板（比如"定时任务 + 邮件通知"）的代码 + workflows + AGENTS.md
- **(d)** GitHub 国内访问加速的具体技术方案（反代 Worker 代码）
